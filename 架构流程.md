
#### 整体架构流程
```txt
前情概要:
meta: (id, mid, file_path,content, updated_at)


event:
①id, 
②mid, 替代原来的inode
③ref_meta (外键)
④file_path 
⑤proc_name
⑥st, et, size_bytes
⑦diff
⑧diff_des
⑨status = ①diffing ②descing ③done ④failed
⑩tmpfilepath: 写入数据库这一刻的文件备份

主线程（eslogger 监听）
  │  收到事件(eslogger)
  │  → [verify管线] verify1:应用匹配
  │  → [verify管线] verify2:全局噪声后缀
  │  → [verify管线] verify3:全局噪声目录
  │  → [verify管线] verify4:各个应用的噪声 后缀/目录 过滤
  │  → [verify管线] verify5:防抖过滤(同一文件2s内不重复处理)
  │  → 产出 file_path(文件的路径), full_proc_path(哪个应用修改)
  │  │ → mid = xattr.get_file_mid(file_path)
  │  │  def deal_mid(mid):
  │  └──│─ mid = "": [首次]做什么:①xattr.update_file_mid(file_path)->new_mid ②存到file_event.db的meta的mid和file_path; 然后调用deal_mid(new_mid)
  │  		└─ mid√ file_path× (meta表mid相同,但是file_path不同) [处理文件复制/重命名/移动....情况]
  │  			 └──── ①xattr.update_file_mid(file_path)->new_mid ②数据库的meta也同步更新mid=new_mid和file_path; 然后重新调用deal_mid(new_mid)
	│  		└─ mid√ file_path√
  │  			 └──│─ 小于merge_threshold_ms ==> [MERGE] 将event最后一条的时间修改下即可
  └──  		    └─ 大于merge_threshold_ms ==> [NEW] 插入event, 把状态改为diff即可, 留给workerA把这个抓来处理diff字段; 主线程不管了
  
Worker A — Diff Worker(轮询event表的status=①diffing 目标:填充 diff + mode 字段)
  │  │ > 用到 reader + diff
  │  │ → newContent = reader.read_file(tmpfilepath)
  │  └──│─ planin 普通文本文件(.py, .ts....)
  │  		└─ markitdown 复杂文件(.word, .ppt, .pdf, .excel...) [可能失败,当前条目status=failed]
  │  │ → oldContent = "SELECT content FROM meta WHERE id = 当前这一条的ref_meta"
  │  └──│─ diff = difflib.unified_diff(old_lines, new_lines); mode = plain
  │  		└─ diff = f"旧内容{oldContent}\n新内容{newContent}    ; mode = llm
  │  │ → 使用newContent去更新meta表的 ①content; ②同时要更新updated_at ③!!!!注意不能更新file_path(否则会有慢线程导致 file_path 回退到旧值。)
  └─── → 删除 tmpfilepath 的文件 + status 由 diffing => descing
  

Worker B — Desc Worker(轮询event表的status=②descing 目的填充 diff_des 字段)
  │  │ → 按照mode去调用 POST /llm/task 去投递 [投递可能失败,当前条目status=failed]
  │  └──│─ mode = plain (diff, DES_PLAIN_PROMPT="这是一个diff内容,你需要总结本次的改动...")
  │  		└─ mode = llm   (diff, DES_LLM_PROMPT="这是新旧文本,你需要总结本次的改动...")
  └─── → status 由 descing => done (投递出去就done,不管后续的)

```

```txt
难点是, 如果mid命中了, 但是file_path不同, 怎么处理?
文件: c.txt(mid=1)→d.txt(mid=1)→ 复制 d'.txt(mid=1)
数据库meta表: mid=1, file_path =c.txt
case1: 用户编辑d.txt, 那查mid, 发现file_path不同, 
solution1: 文件变为 d.txt(mid=2)→ 复制 d'.txt(mid=1) ==>[首次] 然后插入meta表mid=2, file_path=d.txt; loss = 会断追踪! 
solution2: 数据库: mid=1, file_path由c.txt=>d.txt,追踪不断;  loss = 当修改d'.txt时, 你又mid=1, 变file_path, 但是d'.txt的内容会污染, 
也就是只有首次copy出来, 两个文件一样, 你任意编辑一个都OK ,但是如果编辑第二个, 就会产生污染; 

----我想到的解决方法:
solution2 + 
```

| mid  | file_path | 什么情况                                                     |
| ---- | --------- | ------------------------------------------------------------ |
| √    | ×         | ①重命名了<br />②复制导致的                                   |
| √    | √         | 正常                                                         |
| ×    | √         | [首次], mid没装==>从来没有记录过这个文件==>[首次] (不看file_path) |
| ×    | ×         | [首次]                                                       |

```md
文件: c.txt(mid=1)→d.txt(mid=1)→ 复制 d'.txt(mid=1)
数据库meta表: mid=1, file_path =c.txt

STEP1: 修改d.txt, 命中mid=1发现file_path不同 ==>
文件:d.txt(mid=1); d'.txt(mid=1) ; d.txt(mid=2); d'.txt(mid=1)
数据库meta表: mid=2, file_path =d.txt

STEP2: 修改d.txt, 命中mid=2发现file_path相同 ==> 正常
STEP2.1: 修改d'.txt, 发现mid=1数据库没有, 就会把这个文件认为为 [首次](因为没有命中mid)
文件:d.txt(mid=1); d'.txt(mid=1) | d.txt(mid=2); d'.txt(mid=3)
数据库meta表: mid=2, file_path =d.txt | mid=3, file_path = d'.txt

Q1是否会有: 会断追踪问题, 因为d.txt才是真的c.txt的延续, d'不是
因为d和d'是一样的! d'是d复制出来的, 就是没有断的

Q2要是断点操作怎么办: 把daemon关了, 用户编辑了d'.txt
然后下次一开启daemon时, 先编辑的d'.txt, d'.txt会延续c.txt的evnet; 
问题来了, d才是真身,但是被d'延续了c.txt, d在断电时没有编辑, 按理来说c.txt应该延续d的不继续编辑; 
> 也合理, 因为我们daemon就是要求一直开着的, 如果你断了, 并在断时编辑了,我们延续daemon你断前的上一次编辑,也合理!

总结: solution: 当mid命中,file_path没有命中时, ①xattr.update_file_mid(file_path) ②数据库的meta也同步更新mid和file_path
```

|                                                              |                                                              |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| STEP1: 修改d.txt, 命中mid=1发现file_path不同 ==><br />文件:d.txt(mid=1); d'.txt(mid=1) ; d.txt(mid=2); d'.txt(mid=1)<br />数据库meta表: mid=2, file_path =d.txt | STEP1: 修改d'.txt, 命中mid=1发现file_path不同 ==><br />文件:d.txt(mid=1); d'.txt(mid=1) ; d.txt(mid=1); d'.txt(mid=1)<br />数据库meta表: mid=2, file_path =d'.txt |

TODO: 冗余数据库的问题



```md
1. markitdown接入llm的接口
2. markitdown接入语音的接口
```

#### `repository.py`设计

```txt
# ── 连接工厂 ──
get_conn(db_path) -> sqlite3.Connection

# ── 建表（幂等）──
init_db(db_path) -> None

# ── Meta ──
insert_meta(conn, mid, file_path, updated_at) -> int             # 返回 meta.id
get_meta_by_mid(conn, mid) -> dict | None
update_meta(conn, meta_id, **fields)                             # 通用更新

# ── Event ──
get_last_event(conn, meta_id) -> dict | None
insert_event(conn, **fields) -> int                              # 通用插入，返回 event.id
update_event(conn, event_id, **fields)                           # 通用更新

# ── Worker 轮询 ──
poll_events(conn, status, limit) -> list[dict]                   # 按 status 轮询

# ── 文件树（替代旧 discovered_files）──
get_discovered(conn, proc_name) -> list[Row]                     # GROUP BY file_path
```

#### Reader工作流

```
reader( old_content 原meta表的content, tmpfilepath 此条event的快照文件路径) 
  │ → case1: 判断 tmpfilepath 是普通文
  │  │ → new_content = Path(tmpfilepath).read_text(encoding='utf-8') # 纯文本内容
  │  │ → def get_diff(old_content: str, new_content: str) -> str: # 返回 unified diff 文本 方法
              return "".join(difflib.unified_diff(old_content.splitlines(keepends=True), new_content.splitlines(keepends=True)))
  │  │ → return 1.get_diff(old_content, new_content) 2.new_content
  │  │ 
  │ → case2: 判断 tmpfilepath 是markitdown处理
  │  │ → result = md.convert(input_path, keep_data_uris=True) # markitdown处理图片为b64
  │  │ → ①image_dict= {"IMG:md5_1": "图片1的路径", "IMG:md5_2": "图片2的路径"} 
  │  │ → ②new_content = result把里面所有的 图片都换为 `[[IMG:md5_1]]` 这样子
  │  │ → ③last_image_dict = {"IMG:md5_1": "图片描述xxxxxx"}
  │  │ → ④把 image_dict 中md5相同的路径换为 last_image_dict 对应md5的描述; (因为md5相同,没有llm的必要)
  │  │ → ⑤llm_image_dict = {k: vision(v) for k, v in image_dict.items() if Path(v).exists()} 那此时就获取到了所有图片的llm表述
  │  │ → ⑥or_diff = get_diff(old_content, new_content)
  │  │ → ⑦把 or_diff 中所有`[[IMG:md5_1]]`的换为==>`[[IMG:md5_1]] DESC:llm_image_dict['IMG:md5_1']`
  │  │ → ⑧return  1,处理后的or_diff 2.new_content+"#"+json.dumps(llm_image_dict)
  │  │ →
  │  │ →
  │  │ →
  
```

#### 关于日志
原因 ：走的是 [首次] 分支（ eslistener.py:171-177 ）—— insert_meta 后递归 _deal_mid → mid 命中 DB → 进分支 3 → L205 get_last_event(48) 。 此时 meta 刚插入、第一条 event 还没插入 ，查询必然为空。

本质 ：首次路径下"没有上一条事件"本来就是 预期 （直接走 NEW），但 repository.py:196-197 把"空结果"打了 ERROR 级 日志。这是 日志级别错配 ，不是数据问题，功能正常。
```
2026-08-03 08:58:52.116 | INFO    | core.eslistener:175 | [eslistener] [首次] file=/Volumes/SAMSUNG_1T/Documents/CodeBeach/Hermes_ASSISTANT/5_edit_monitor_magic/ref/mid_test_resource/docx/0858_test.docx mid=7519e674-de2c-4415-afc7-cf239f03f9dd
2026-08-03 08:58:52.124 | ERROR   | db.repository:198 | 注意! 请查看48,该项的event为空
2026-08-03 08:58:52.126 | DEBUG   | db.repository:219 | [EVENT] 新增 id=280 ref_meta=48 proc=WPS Office st=2026-08-03T08:58:51
```

#### 支持的功能
> 别人的 https://tableconvert.com/zh-cn/excel-to-markdown 
> 样例: https://samplelib.com/sample-ppt.html
* docx: 文本 + 图片
* excel: 文本
* ppt: 逐元素图片提取
* pdf: 每页一张png=>丢给llm处理

#### 后续支持
1. 音频的解析
2. 更多的文件类似 psd.....

